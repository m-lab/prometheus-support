apiVersion: 1
providers:
- name: 'default'
  orgId: 1
  folder: ''
  type: file
  disableDeletion: true
  # Grafana will scan for changed dashboards every 60s.
  updateIntervalSeconds: 60
  options:
    path: /etc/dashboards
