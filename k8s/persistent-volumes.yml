kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: auto-prometheus-disk0
  annotations:
    volume.beta.kubernetes.io/storage-class: "slow"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 400Gi

